#![cfg(test)]

use super::*;
use frame_support::{assert_noop, assert_ok};
use mock::{Event, *};
use primitives::BlockNumber;

#[test]
#[ignore]
// Creating proposal work
fn create_new_proposal_work() {
    ExtBuilder::default().build().execute_with(|| {
        
    });
}

#[test]
#[ignore]
// Cancelling proposal work
fn cancel_proposal_work() {
    ExtBuilder::default().build().execute_with(|| {
        
    });
}

#[test]
#[ignore]
// Voting works
fn vote_work() {
    ExtBuilder::default().build().execute_with(|| {
        
    });
}

#[test]
#[ignore]
// Remove vote works
fn remove_vote_work() {
    ExtBuilder::default().build().execute_with(|| {
        
    });
}

#[test]
#[ignore]
// Emergency canceling of referendum works
fn emergency_cancel_referendum_work() {
    ExtBuilder::default().build().execute_with(|| {
       
    });
}

